{{- if .Values.ranking.ingress.enabled -}}
{{- $fullName := include "itinera.fullname" . -}}
{{- $svcPort := .Values.ranking.service.port -}}
{{- if and .Values.ranking.ingress.className (not (semverCompare ">=1.18-0" .Capabilities.KubeVersion.GitVersion)) }}
  {{- if not (hasKey .Values.ranking.ingress.annotations "kubernetes.io/ingress.class") }}
  {{- $_ := set .Values.ranking.ingress.annotations "kubernetes.io/ingress.class" .Values.ranking.ingress.className}}
  {{- end }}
{{- end }}
{{- if semverCompare ">=1.19-0" .Capabilities.KubeVersion.GitVersion -}}
apiVersion: networking.k8s.io/v1
{{- else if semverCompare ">=1.14-0" .Capabilities.KubeVersion.GitVersion -}}
apiVersion: networking.k8s.io/v1beta1
{{- else -}}
apiVersion: extensions/v1beta1
{{- end }}
kind: Ingress
metadata:
  name: {{ $fullName }}-ranking
  labels:
    {{- include "itinera.ranking.labels" . | nindent 4 }}
  {{- with .Values.ranking.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if and .Values.ranking.ingress.className (semverCompare ">=1.18-0" .Capabilities.KubeVersion.GitVersion) }}
  ingressClassName: {{ .Values.ranking.ingress.className }}
  {{- end }}
  {{- if .Values.ranking.ingress.tls }}
  tls:
    {{- range .Values.ranking.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.ranking.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            {{- if and .pathType (semverCompare ">=1.18-0" $.Capabilities.KubeVersion.GitVersion) }}
            pathType: {{ .pathType }}
            {{- end }}
            backend:
              {{- if semverCompare ">=1.19-0" $.Capabilities.KubeVersion.GitVersion }}
              service:
                name: {{ $fullName }}-ranking
                port:
                  number: {{ $svcPort }}
              {{- else }}
              serviceName: {{ $fullName }}-ranking
              servicePort: {{ $svcPort }}
              {{- end }}
          {{- end }}
    {{- end }}
{{- end }}
