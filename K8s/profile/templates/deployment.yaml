apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "profile.fullname" . }}
  labels:
    {{- include "profile.labels" . | nindent 4 }}
spec:
  {{- if not .Values.autoscaling.enabled }}
  replicas: {{ .Values.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "profile.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      {{- with .Values.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "profile.selectorLabels" . | nindent 8 }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "profile.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: {{ .Chart.Name }}
          env: 
          - name: SEARCH_URL
            value: {{.Values.global.searchUrl}}
          - name: REVIEW_URL
            value: {{.Values.global.reviewUrl}}
          - name: PHOTO_URL
            value: {{.Values.global.photoUrl}}
          - name: RANKING_URL
            value: {{.Values.global.rankingUrl}}
          - name: DB_USER
            value: {{.Values.global.db.user}}
          - name: DB_NAME
            value: {{.Values.global.db.name}}
          - name: DB_PASSWORD
            value: {{.Values.global.db.password}}
          - name: DB_HOST
            value: {{.Values.global.db.host}}
          - name: DB_PORT  
            value: "{{.Values.global.db.port}}"
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.service.port }}
              protocol: TCP
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
